# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend Configuration (must start with VITE_ for Vite)
VITE_API_URL=http://localhost:3000/api

# Database Configuration
# DB_PATH is auto-resolved, but can override if needed
# DB_PATH=./src/backend/storage/DBStorage/unimartDB.db
